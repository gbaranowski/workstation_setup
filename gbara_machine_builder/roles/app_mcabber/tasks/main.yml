---

- name: install
  become: true
  become_user: root
  apt: name=mcabber state=present
  register: presence

- include_vars: vault.yml

- name: set config
  template: src=mcabberrc.j2 dest=~/.mcabberrc

- name:
  file:
    path: ~/.mcabber/histo
    mode: 0700
    state: directory
